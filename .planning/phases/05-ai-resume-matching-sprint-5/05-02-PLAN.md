# Phase 5, Plan 2: Scoring & Interview Engines (HIQ-043, HIQ-044, HIQ-045, HIQ-047, HIQ-048, HIQ-049)

**Target Phase:** 05-ai-resume-matching-sprint-5
**Goal:** Build the TF-IDF Candidate matching engine and SBERT sentence-transformers interview evaluator in the `ai_service`.

## Technical Context
- **Dependencies:** 05-01-PLAN.md (Lifespan model loading in `ai_service/main.py`), 05-RESEARCH.md (TF-IDF and SBERT usage).
- **New files:** `ai_service/app/services/matching.py`, `ai_service/app/services/interview.py`
- **Modify:** `ai_service/main.py`

## Action Plan

### 1. TF-IDF Job Matching Engine (`ai_service/app/services/matching.py`)
- [ ] Create `compute_match_score(resume_text: str, job_description: str) -> float`.
- [ ] Initialize `TfidfVectorizer(stop_words='english')`.
- [ ] Apply `fit_transform` and compute `cosine_similarity`.
- [ ] Return rounded float score (e.g. 0.8521).

### 2. SBERT Semantic Scoring (`ai_service/app/services/interview.py`)
- [ ] Update `ai_service/main.py` lifespan to preload `SentenceTransformer("sentence-transformers/all-MiniLM-L6-v2")` into `app.state.sbert`.
- [ ] Create `compute_interview_score(candidate_answer: str, ideal_answer: str, sbert_model) -> float`.
- [ ] Implement `sbert_model.encode()` returning tensors, and `util.cos_sim` from `sentence_transformers`.
- [ ] Return rounded scalar score representing semantic similarity.

### 3. API Endpoints
- [ ] Add `POST /match` to `ai_service/main.py`: Accepts `resume_text` and `job_text`, returns `{"score": 0.85}`.
- [ ] Add `POST /evaluate-interview` to `ai_service/main.py`: Accepts `candidate_answer` and `ideal_answer`, returns `{"semantic_score": 0.91}`.

## Verification
- [ ] `POST /match` correctly computes deterministic similarity logic between two texts.
- [ ] `POST /evaluate-interview` scores two semantically similar sentences (e.g. "I optimized SQL" and "I tuned the database queries") high, even if they share zero exact keywords.

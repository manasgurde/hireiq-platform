# Phase 6, Plan 1: Job Seeker UI (HIQ-051, HIQ-053, HIQ-054)

**Target Phase:** 06-dashboards-ai-ui-production-deploy-sprint-6
**Goal:** Implement the React/Next.js UI for candidates to view jobs, apply, upload resumes, and track their application statuses.

## Technical Context
- **Dependencies:** Web app configured in Phase 3. Core API `applications` and `resumes` endpoints.
- **Components:** `CandidateDashboard`, `ResumeUploader`, `ApplicationStatusTracker`.

## Action Plan
- [ ] Create `frontend/src/pages/candidate/dashboard.tsx`.
- [ ] Build a sleek `ResumeUploader` component with drag-and-drop capability, connecting to the pre-signed S3 URL API.
- [ ] Build the `ApplicationStatusTracker` table showing applications with colored status badges (e.g., Pending, Interview, Rejected).
- [ ] Implement a circular "Profile Strength / AI Match Score" SVG ring showing how well their profile matches a specific recommended job.

## Verification
- [ ] Candidate can log in, drag and drop a PDF, and see the file successfully upload to S3 and parse.

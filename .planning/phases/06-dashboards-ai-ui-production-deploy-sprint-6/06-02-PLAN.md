# Phase 6, Plan 2: Recruiter UI (HIQ-052, HIQ-057)

**Target Phase:** 06-dashboards-ai-ui-production-deploy-sprint-6
**Goal:** Implement the Recruiter Dashboard for managing active job postings and reviewing inbound candidates with AI scores.

## Technical Context
- **Dependencies:** Web app configured in Phase 3.
- **Components:** `RecruiterDashboard`, `CandidatesTable`, `CandidateSlideOver`.

## Action Plan
- [ ] Create `frontend/src/pages/recruiter/dashboard.tsx`.
- [ ] Build the `CandidatesTable` to fetch all applications for a specific job, sorting them descending by the `ai_match_score`.
- [ ] Build a `CandidateSlideOver` (drawer component) that opens when a recruiter clicks a row, showing the parsed resume skills, organizations, and the raw PDF link.
- [ ] Add action buttons: "Advance to Interview" and "Reject", updating the application status via the backend API.

## Verification
- [ ] Recruiter can view candidates sorted by AI score, open the drawer, and successfully change a candidate's status to Interview.

# Phase 6, Plan 3: AI Insights & Interview UI (HIQ-055, HIQ-056)

**Target Phase:** 06-dashboards-ai-ui-production-deploy-sprint-6
**Goal:** Implement the mock AI interview chat interface and the AI skill gap insights page.

## Technical Context
- **Dependencies:** 06-01-PLAN.md (Applicant tracking). AI service `/evaluate-interview` endpoint.
- **Components:** `AIChatInterface`, `SkillGapRadar`.

## Action Plan
- [ ] Create `frontend/src/pages/candidate/interview/[id].tsx` for the mock interview.
- [ ] Build the `AIChatInterface` simulating a text-based interview. It prompts the user with 3 behavioral questions one by one.
- [ ] On submit, hit the backend to evaluate the answers using the SBERT engine and display the semantic score dynamically on the right sidebar.
- [ ] Create a `SkillGapRadar` using Recharts (`RadarChart`) comparing the candidate's parsed skills against the job posting's required skills.

## Verification
- [ ] Submitting an interview answer instantly returns a score in the UI sidebar without page reload.

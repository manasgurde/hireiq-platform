# Phase 6, Plan 4: Production AWS Deploy & Analytics (HIQ-058, HIQ-059, HIQ-060)

**Target Phase:** 06-dashboards-ai-ui-production-deploy-sprint-6
**Goal:** Add Recharts analytics for recruiters, write the AWS Terraform config, and do a final security audit.

## Technical Context
- **Dependencies:** Recharts library. Terraform CLI.
- **Components:** `AnalyticsDashboard` (frontend), `infrastructure/main.tf` (DevOps).

## Action Plan
- [ ] **Analytics:** Add `Recharts` `BarChart` and `LineChart` to the Recruiter Dashboard showing "Applications over Time" and "Average Match Score per Job".
- [ ] **Terraform:** Create `infrastructure/main.tf` and `infrastructure/variables.tf`.
- [ ] Define AWS VPC, public/private subnets, Security Groups, ALB, ECS Fargate cluster, and RDS PostgreSQL instance.
- [ ] Define ElastiCache Redis for the AI caching layer.
- [ ] **Security:** Run an OWASP Top 10 automated audit script or manual checklist verifying JWT handling, CORS, SQL injection prevention (SQLAlchemy does this), and secure S3 presigned URLs.

## Verification
- [ ] `terraform plan` executes successfully and shows the correct desired AWS architecture state.
